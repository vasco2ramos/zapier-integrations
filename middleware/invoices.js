var later = require('later');



exports.startInvoices = function(req){
  // Get connection
  hiveage.newConnection();
  // Check date for invoicing
    // Get deal details (need to be parsed)
  // schedule it
  // Send slack message ()
}
